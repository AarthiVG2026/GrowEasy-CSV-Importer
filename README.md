# GrowEasy-CSV-Importer
An AI-powered CSV Importer that uses Google Gemini 2.5 Flash to automatically map arbitrary CSV files and messy lead data into a strict CRM schema, featuring a premium Next.js dashboard.
